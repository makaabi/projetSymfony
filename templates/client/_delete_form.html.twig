<form method="post" action="{{ path('client_delete', {'id_cli': client.id_cli}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id_cli) }}">
    <button class="btn">Delete</button>
</form>
